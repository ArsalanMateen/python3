'''
The assert is used when debugging code.
The assert lets you test if a condition in your code returns True, if not, the program will raise an AssertionError.
'''
numerator = 10
denominator = 0

assert denominator != 0 # check for division by zero
division = numerator / denominator