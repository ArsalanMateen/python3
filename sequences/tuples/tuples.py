'''
Tuples are immutable sequences, typically used to store collections of heterogeneous data (of different data types, structures, formats or sources). 
They are defined by enclosing the elements in parentheses ().
'''

tuples = ('Arsalan', {'degree': 'BCS', 'gpa': 3.15}, 22, ['Python3', 'MERN', 'C++'])
print (tuples)
print (len(tuples[1]))